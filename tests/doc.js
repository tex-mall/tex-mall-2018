/**
 * @api {post} /passport/verify_login 验证码登录
 * @apiVersion 0.1.0
 * @apiName VerifyLogin
 * @apiGroup Passport
 * @apiExample Example usage:
 * curl -i http://localhost/passport/verify_login
 *
 * @apiParam {Number} mobile 手机号码.
 * @apiParam {Number} sms_code 短信验证码.
 *
 */
function verifyLogin() { return; }

/**
 * @api {post} /passport/rest_password 修改密码
 * @apiVersion 0.1.0
 * @apiName RestPassword
 * @apiGroup Passport
 * @apiExample Example usage:
 * curl -i http://localhost/passport/rest_password
 *
 * @apiParam {Number} mobile 手机号码.
 * @apiParam {Number} sms_code 短信验证码.
 * @apiParam {String} password 密码.
 *
 */
function restPassword() { return; }
